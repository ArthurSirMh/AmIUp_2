// import { Router } from "express";

// import { fetchDataUser, createUser } from "../controllers/user.controller";

// const router = Router();
// router.get("/test", (req, res) => {
//     res.json({ ok: true });
// });

// router.post("/users", createUser);
// router.get("/users", fetchDataUser);

// export default router;